import { Link, useRoute } from "wouter"; import { ArrowLeft, Check, LockKeyhole, ShieldCheck } from "lucide-react"; import { getProduct } from "@shared/catalog";
export default function Product(){const [,params]=useRoute("/product/:slug");const p=params?.slug?getProduct(params.slug):undefined;if(!p)return <main className="catalog-page"><section className="catalog-hero"><h1>Produto não encontrado</h1><Link href="/catalog">Voltar</Link></section></main>;const checkout=p.id==="sensi-normal"?"sensiNormal":p.id==="sensi-premium"?"sensiPremium":"sensiEmulator";return <main className="catalog-page"><header className="checkout-header"><Link href="/catalog" className="back-link"><ArrowLeft size={16}/> Catálogo</Link><span className="brand"><span className="brand-mark">SK$</span><span>STORE</span></span></header><section className="product-detail"><span className="catalog-category">{p.category} · atualizado {p.updatedAt}</span><h1>{p.name}</h1><p className="product-lead">{p.description}</p><div className="product-columns"><div className="product-panel"><h2>O que você recebe</h2><ul>{p.included.map(x=><li key={x}><Check size={16}/>{x}</li>)}</ul><h2>Compatibilidade</h2><div className="catalog-tags">{p.compatibility.map(x=><span key={x}>{x}</span>)}</div></div><aside className="product-buy"><ShieldCheck size={22}/><strong>R$ {(p.priceCents/100).toFixed(2).replace(".",",")}</strong><p>Entrega protegida após aprovação no Discord.</p><Link className="plan-button" href={`/checkout/${checkout}`}>Comprar agora</Link><small><LockKeyhole size={13}/> Acesso vinculado à sua conta</small></aside></div></section></main>}

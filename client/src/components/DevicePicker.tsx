import { DEVICES, type DeviceProfile } from '../../../shared/devices';
export function DevicePicker({value,onChange}:{value:DeviceProfile;onChange:(v:DeviceProfile)=>void}) {
  return <fieldset className="device-picker"><legend>01 / Seu aparelho</legend><p>Escolha o celular para identificar seu guia. Os valores são pontos de partida: você ajusta no treino conforme seu toque.</p><label>Marca<select value={value.brand} onChange={e=>onChange({...value,brand:e.target.value,model:''})}>{Object.keys(DEVICES).map(b=><option key={b}>{b}</option>)}</select></label><label>Modelo<input list="device-models" maxLength={100} value={value.model} placeholder="Busque ou digite o modelo completo" onChange={e=>onChange({...value,model:e.target.value})}/><datalist id="device-models">{DEVICES[value.brand].map(m=><option key={m} value={m}/>)}</datalist></label><div className="device-options"><label>Jogo<select value={value.game} onChange={e=>onChange({...value,game:e.target.value})}><option>Free Fire</option><option>Free Fire MAX</option></select></label><label>Preferência<select value={value.style} onChange={e=>onChange({...value,style:e.target.value})}><option>Equilibrado</option><option>Mais controle</option><option>Movimentos rápidos</option></select></label></div><small>Confirme que o jogo já roda no aparelho. Estar nesta lista não garante compatibilidade ou desempenho.</small></fieldset>;
}

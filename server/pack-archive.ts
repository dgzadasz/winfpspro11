import type { DeviceProfile } from '../shared/devices';
import { readFileSync } from 'node:fs';
import path from 'node:path';

const escape = (v:string) => v.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
// Small, uncompressed ZIP writer. Names are internal constants, never user input.
export function zipFiles(files:Record<string,string>):Buffer {
  const chunks:Buffer[]=[], directory:Buffer[]=[]; let offset=0;
  for(const [name,content] of Object.entries(files)) {
    const n=Buffer.from(name), data=Buffer.from(content); let crc=0xffffffff;
    for(let j=0;j<data.length;j++){crc^=data[j];for(let i=0;i<8;i++)crc=(crc>>>1)^((crc&1)?0xedb88320:0);}
    crc=(crc^0xffffffff)>>>0;
    const h=Buffer.alloc(30);h.writeUInt32LE(0x04034b50);h.writeUInt16LE(20,4);h.writeUInt32LE(crc,14);h.writeUInt32LE(data.length,18);h.writeUInt32LE(data.length,22);h.writeUInt16LE(n.length,26);
    const d=Buffer.alloc(46);d.writeUInt32LE(0x02014b50);d.writeUInt16LE(20,4);d.writeUInt16LE(20,6);d.writeUInt32LE(crc,16);d.writeUInt32LE(data.length,20);d.writeUInt32LE(data.length,24);d.writeUInt16LE(n.length,28);d.writeUInt32LE(offset,42);
    chunks.push(h,n,data);directory.push(d,n);offset+=h.length+n.length+data.length;
  }
  const central=Buffer.concat(directory), end=Buffer.alloc(22);end.writeUInt32LE(0x06054b50);end.writeUInt16LE(Object.keys(files).length,8);end.writeUInt16LE(Object.keys(files).length,10);end.writeUInt32LE(central.length,12);end.writeUInt32LE(offset,16);
  return Buffer.concat([...chunks,central,end]);
}
const html=(title:string,body:string)=>`<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)}</title><style>body{font:17px/1.7 system-ui;background:#100c17;color:#eee;max-width:800px;margin:auto;padding:24px}h1,h2{color:#cf9fff}table{width:100%;border-collapse:collapse}td{border-bottom:1px solid #534060;padding:8px}a{color:#bdcfff}pre{white-space:pre-wrap;font:inherit}.note{background:#281d35;padding:16px;border-radius:12px}</style><h1>${escape(title)}</h1>${body}</html>`;
export function deviceGuide(p:DeviceProfile) {
  const adjustment=p.style==='Mais controle'?-15:p.style==='Movimentos rápidos'?10:0;
  const values=[['Geral',160],['Ponto vermelho',150],['Mira 2x',140],['Mira 4x',125],['AWM',70],['Olhadinha',110]] as const;
  const apple=p.brand==='Apple';
  return html(`Seu guia · ${p.brand} ${p.model}`,`<p>${escape(p.game)} · ${escape(p.style)}</p><p class="note">Perfil inicial para calibração manual. Não foi testado fisicamente neste modelo. Modelos diferentes podem partir dos mesmos valores; tela, FPS, HUD e seu movimento determinam o ajuste final. Não há sensibilidade universal ou garantia de acertos.</p><h2>1. Prepare o aparelho</h2><p>Confira o modelo em ${apple?'Ajustes → Geral → Sobre':'Configurações → Sobre o telefone (o nome varia pela marca e versão)'}. No jogo, comece com gráficos suaves e uma taxa de quadros que permaneça estável. Se aquecer ou travar, reduza os gráficos. Mantenha a resolução e o tamanho de exibição originais enquanto calibra.</p><h2>2. Insira no jogo</h2><p>Abra Configurações → Sensibilidade. Anote ou fotografe seus valores antigos antes de alterar. Estes pontos iniciais usam a escala de 0 a 200:</p><table>${values.map(([n,v])=>`<tr><td>${n}</td><td>${v+adjustment}</td></tr>`).join('')}</table><h2>3. Botão e HUD</h2><p>Em Controles → HUD personalizado, experimente começar com o botão de disparo em 50%. Posicione onde seu dedo alcança sem esforço e sem encobrir a mira. Teste 45% e 55%, mantendo as outras opções iguais; escolha pelo conforto e pela consistência, não pelo número de outro jogador.</p><h2>4. Calibre em cinco minutos</h2><ol><li>No treino, faça dez movimentos de mira iguais, primeiro sem luneta.</li><li>Se passar do alvo repetidamente, reduza Geral em 5. Se faltar movimento, aumente em 5.</li><li>Repita com ponto vermelho, 2x e 4x, alterando somente a mira em teste.</li><li>Compare três séries; mantenha o ajuste com menos correções. Não ultrapasse 0–200.</li><li>Salve uma captura do HUD e dos números. Refaça após mudar FPS, resolução, película ou versão do jogo.</li></ol><h2>5. Toque e acessibilidade</h2>${apple?'<p>No iPhone, Ajustes → Acessibilidade → Toque reúne as opções de toque. Toque Tátil altera o tempo de pressionar para ações de contexto; não aumenta a sensibilidade da mira. Adaptações de Toque → Duração ao Pressionar pode atrasar o reconhecimento. Não ative isso como truque de desempenho. Preserve ajustes de acessibilidade de que você precisa e registre o estado anterior antes de experimentar.</p><p><a href="https://support.apple.com/pt-br/102222">Apple: Adaptações de Toque</a> · <a href="https://support.apple.com/pt-br/guide/iphone/iph77bcdd132/26/ios/26">Apple: ajustes de toque</a></p>':'<p>No Android, o atraso de tocar e pressionar controla gestos longos, não a frequência da tela nem a sensibilidade do Free Fire. Não altere DPI em opções de desenvolvedor como promessa de mira melhor. Os nomes dos menus variam por fabricante; use a busca das Configurações. Preserve adaptações de acessibilidade necessárias.</p><p><a href="https://support.google.com/accessibility/android/answer/6006989?hl=pt-BR">Google: atraso ao tocar e pressionar</a></p>'}<h2>6. Como abrir</h2><p>Extraia o ZIP no app Arquivos do iPhone, gerenciador de arquivos Android ou Explorador do Windows. Abra este HTML no navegador. Não é APK e não altera o jogo automaticamente.</p><p><a href="https://ff.garena.com/pt/article/1343/">Garena: escala de sensibilidade</a></p>`);
}
export function personalizedPack(profile:DeviceProfile, premium:boolean) {
 const read=(name:string)=>readFileSync(path.resolve(process.cwd(),'server/private-packs',name),'utf8');
 return zipFiles({'COMECE-AQUI.html':deviceGuide(profile),'GUIA-CELULAR.html':html('Guia celular',`<pre>${escape(read(premium?'sensi-premium.md':'sensi-normal.md'))}</pre>`),'LEIA-ME.txt':'Extraia o ZIP e abra COMECE-AQUI.html no navegador. O tutorial não instala nem modifica o jogo. Guarde seus ajustes anteriores.'});
}
export function emulatorGuide() { return html('Guia para PC e emulador', '<p class="note">Este material é exclusivo para mouse, teclado, DPI, resolução e controles do emulador. Não use o perfil de celular no PC.</p><h2>Comece assim</h2><ol><li>Escolha uma resolução estável e mantenha a mesma durante o teste.</li><li>Registre seu DPI e a sensibilidade do emulador antes de alterar.</li><li>Faça movimentos iguais no campo de treino e ajuste somente uma opção por vez.</li><li>Se houver travamentos, reduza a qualidade gráfica antes de mexer na mira.</li></ol><p>O guia não instala APK, macro, script ou alteração de arquivos.</p>'); }
